name: Publish
run-name: Release ${{ github.ref_name }}

on:
  release:
    types:
      - published

jobs:
  publish:
    name: Publish
    uses: jhae-de/jhae-de/.github/workflows/publish-node-package.yaml@main
    with:
      needs-build: false
    secrets:
      node-auth-token: ${{ secrets.NODE_AUTH_TOKEN }}
